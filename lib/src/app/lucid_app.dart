import 'package:flutter/widgets.dart';

// TODO: Create optiona, con
class LucidApp extends StatelessWidget {
  const LucidApp({super.key});

  @override
  Widget build(BuildContext context) {
    return WidgetsApp(
      // App color in switcher.
      color: Color(0xFFFFFFFF),
    );
  }
}
